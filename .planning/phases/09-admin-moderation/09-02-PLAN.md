---
phase: 09-admin-moderation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/events/event.entity.ts
  - src/events/event-audit-log.entity.ts
  - src/events/dto/admin-event-query.dto.ts
  - src/events/dto/admin-event-moderation.dto.ts
  - src/events/dto/paginated-admin-events-response.dto.ts
  - src/database/migrations/1751000000000-admin-event-status.ts
  - src/database/migrations/1751000000001-admin-audit-log.ts
autonomous: true
requirements: [EVT-03, ADMIN-02, ADMIN-04]
must_haves:
  truths:
    - "EventStatus enum includes SUSPENDED as an admin-only state"
    - "EventEntity has a nullable statusBeforeSuspension column for restore"
    - "event_audit_log table exists mirroring organizer_audit_log with eventId, action enum, adminUserId, note"
    - "Two migrations exist: enum/column extension (transaction=false) and audit-log creation"
  artifacts:
    - path: "src/events/event-audit-log.entity.ts"
      provides: "EventAuditLogEntity + EventAuditAction enum"
      contains: "EventAuditAction"
    - path: "src/events/event.entity.ts"
      provides: "EventStatus.SUSPENDED + statusBeforeSuspension column"
      contains: "SUSPENDED"
    - path: "src/events/dto/admin-event-query.dto.ts"
      provides: "AdminEventQueryDto (cursor, limit, status, organizerId, includeDeleted)"
      contains: "AdminEventQueryDto"
    - path: "src/events/dto/admin-event-moderation.dto.ts"
      provides: "AdminEventModerationDto (note)"
      contains: "AdminEventModerationDto"
    - path: "src/events/dto/paginated-admin-events-response.dto.ts"
      provides: "PaginatedAdminEventsResponseDto wrapping full EventEntity[]"
      contains: "PaginatedAdminEventsResponseDto"
    - path: "src/database/migrations/1751000000000-admin-event-status.ts"
      provides: "ALTER TYPE ADD VALUE SUSPENDED + statusBeforeSuspension column"
      contains: "transaction = false"
    - path: "src/database/migrations/1751000000001-admin-audit-log.ts"
      provides: "CREATE event_audit_log + ALTER organizer_audit_log ADD adminUserId"
      contains: "event_audit_log"
  key_links:
    - from: "src/events/event.entity.ts statusBeforeSuspension"
      to: "migration 1751000000000 statusBeforeSuspension column"
      via: "name: 'statusBeforeSuspension' matches DDL"
      pattern: "statusBeforeSuspension"
    - from: "src/events/event-audit-log.entity.ts"
      to: "migration 1751000000001 event_audit_log table"
      via: "entity column names match DDL"
      pattern: "event_audit_log"
---

<objective>
Build the database/entity layer for admin event moderation: extend EventStatus with SUSPENDED (admin-only, D-01), add statusBeforeSuspension for reversible restore (D-02/D-03), create the EventAuditLogEntity + table (D-13), add adminUserId to organizer_audit_log (D-12), and create the admin event DTOs (D-06–D-09, D-14). No behavior yet — that lands in 09-03. This plan is purely schema + types + migration files (migration EXECUTION is the BLOCKING gate in 09-04).

Purpose: Provide the contracts and schema that AdminEventsService (09-03) builds against, defined first to avoid a scavenger hunt.
Output: Extended EventEntity, new EventAuditLogEntity, three admin DTOs, two migration files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-admin-moderation/09-CONTEXT.md
@.planning/phases/09-admin-moderation/09-PATTERNS.md
@.planning/phases/09-admin-moderation/09-RESEARCH.md

# Direct analogs (read before writing)
@src/organizers/organizer-audit-log.entity.ts
@src/events/event.entity.ts
@src/events/dto/event-pagination-query.dto.ts
@src/events/dto/paginated-events-response.dto.ts
@src/organizers/dto/approve-organizer.dto.ts
@src/database/migrations/1747000000000-organizers.ts

# CRITICAL: 09-RESEARCH.md Pitfall 1 — `ALTER TYPE event_status ADD VALUE` CANNOT run inside a
# transaction on PostgreSQL/Neon. The migration class MUST declare `public readonly transaction = false`.
# CRITICAL: 09-RESEARCH.md Pitfall 2 — every new camelCase column needs `name:` on @Column to prevent
# TypeORM sync from creating a duplicate column (Phase 7 lesson).
</context>

<tasks>

<task type="auto">
  <name>Task 1: EventAuditLogEntity + EventStatus.SUSPENDED + statusBeforeSuspension</name>
  <files>src/events/event-audit-log.entity.ts, src/events/event.entity.ts</files>
  <read_first>
    src/organizers/organizer-audit-log.entity.ts (exact template),
    src/events/event.entity.ts lines 1-40 (enum + entity head),
    09-PATTERNS.md "event-audit-log.entity.ts" and "event.entity.ts (extend)" sections
  </read_first>
  <action>
    Create EventAuditLogEntity mirroring OrganizerAuditLogEntity (D-13): @Entity('event_audit_log'); columns id (varchar 30 PK, @BeforeInsert createId), eventId (varchar 30, name: 'eventId'), action (enum EventAuditAction with enumName: 'event_audit_action' to avoid collision with organizer_audit_action), note (varchar 2000 nullable, SEC-01), adminUserId (varchar 30, name: 'adminUserId' — NOT NULL on the new table, always set), createdAt (@CreateDateColumn). Export `enum EventAuditAction { SUSPENDED = 'suspended', RESTORED = 'restored', REMOVED = 'removed' }`. Add @ApiProperty/@ApiPropertyOptional on every field (Phase 5 Swagger lesson).
    Extend EventEntity: add `SUSPENDED = 'SUSPENDED'` to the EventStatus enum (D-01). Add a nullable column `statusBeforeSuspension: EventStatus | null` using `@Column({ type: 'enum', enum: EventStatus, enumName: 'event_status', nullable: true, name: 'statusBeforeSuspension' })` with @ApiPropertyOptional (D-02/D-03). The `name:` is mandatory (Pitfall 2). Do NOT add the audit entity to events.module here — that wiring is 09-03.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "event-audit-log|event\.entity" | grep -v "^$"; npm run build &gt; /dev/null 2>&1 &amp;&amp; echo BUILD-OK</automated>
  </verify>
  <done>EventAuditLogEntity compiles; EventStatus has SUSPENDED; statusBeforeSuspension column declared with explicit name; build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Admin event DTOs (query, moderation, paginated response)</name>
  <files>src/events/dto/admin-event-query.dto.ts, src/events/dto/admin-event-moderation.dto.ts, src/events/dto/paginated-admin-events-response.dto.ts</files>
  <read_first>
    src/events/dto/event-pagination-query.dto.ts,
    src/events/dto/paginated-events-response.dto.ts,
    src/organizers/dto/approve-organizer.dto.ts,
    09-PATTERNS.md DTO sections (admin-event-query, admin-event-moderation)
  </read_first>
  <action>
    AdminEventQueryDto (D-06–D-09): cursor?: string, limit?: number (@Min(1)@Max(100)@Type(()=>Number)), status?: EventStatus (@IsEnum — accepts SUSPENDED), organizerId?: string, includeDeleted?: boolean (@Transform(({value}) => value === 'true' || value === true) — query strings arrive as text). All @IsOptional + @ApiPropertyOptional.
    AdminEventModerationDto (D-14): note?: string with @IsOptional @IsString @MaxLength(2000) (SEC-01) + @ApiPropertyOptional. Shared by suspend/restore/remove.
    PaginatedAdminEventsResponseDto (D-08): { data: EventEntity[]; nextCursor: string | null; hasMore: boolean } typed to the FULL EventEntity (not EventResponseDto — admin must see status, statusBeforeSuspension, deletedAt, organizerId). @ApiProperty({ type: [EventEntity] }).
  </action>
  <verify>
    <automated>npm run build &gt; /dev/null 2>&1 &amp;&amp; echo BUILD-OK</automated>
  </verify>
  <done>Three DTOs compile; response DTO is typed to full EventEntity; moderation note capped at 2000.</done>
</task>

<task type="auto">
  <name>Task 3: Migration files — enum/column (transaction=false) and audit-log creation</name>
  <files>src/database/migrations/1751000000000-admin-event-status.ts, src/database/migrations/1751000000001-admin-audit-log.ts</files>
  <read_first>
    src/database/migrations/1747000000000-organizers.ts (DDL style),
    09-PATTERNS.md migration sections (both files, verbatim DDL),
    09-RESEARCH.md Pitfall 1, Pitfall 5
  </read_first>
  <action>
    1751000000000-admin-event-status.ts: class AdminEventStatus1751000000000 with `public readonly transaction = false;` (Pitfall 1 — mandatory). up(): `ALTER TYPE "event_status" ADD VALUE IF NOT EXISTS 'SUSPENDED'`, then `ALTER TABLE "events" ADD COLUMN IF NOT EXISTS "statusBeforeSuspension" "event_status" NULL`. down(): drop the column only (PostgreSQL cannot DROP an enum value without a full type recreate — leave the value).
    1751000000001-admin-audit-log.ts: class AdminAuditLog1751000000001 (default transactional). up(): CREATE TYPE "event_audit_action" AS ENUM ('suspended','restored','removed'); CREATE TABLE "event_audit_log" (id varchar(30) PK, eventId varchar(30) NOT NULL, action event_audit_action NOT NULL, note varchar(2000) NULL, adminUserId varchar(30) NULL, createdAt TIMESTAMPTZ NOT NULL DEFAULT now(), FK eventId → events(id) ON DELETE CASCADE, FK adminUserId → users(id) ON DELETE SET NULL); CREATE INDEX on (eventId) and (createdAt); then `ALTER TABLE "organizer_audit_log" ADD COLUMN IF NOT EXISTS "adminUserId" varchar(30) NULL REFERENCES "users"("id") ON DELETE SET NULL` (D-12 — nullable, no backfill, Pitfall 5). down(): reverse in inverse order (drop organizer column, drop indexes, drop table, drop type). Use IF NOT EXISTS / IF EXISTS where PostgreSQL allows to be Neon-drift tolerant.
    Note: event_audit_log.adminUserId is NULL-able at the DB level (FK SET NULL on user delete) even though the entity declares it required for new rows — this matches the FK behavior and avoids orphan-delete failures.
  </action>
  <verify>
    <automated>npm run build &gt; /dev/null 2>&1 &amp;&amp; grep -q "transaction = false" src/database/migrations/1751000000000-admin-event-status.ts &amp;&amp; grep -q "event_audit_log" src/database/migrations/1751000000001-admin-audit-log.ts &amp;&amp; echo MIGRATIONS-OK</automated>
  </verify>
  <done>Both migration files compile; enum migration sets transaction=false; audit migration creates table + indexes + organizer_audit_log column. Migrations are NOT run here (09-04 BLOCKING gate runs them).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration runner → Neon PostgreSQL | DDL must apply cleanly; enum extension forbidden inside a transaction |
| event_audit_log.adminUserId → users.id | FK integrity for non-repudiation trail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Denial of Service | enum-extension migration | mitigate | `transaction = false` on the migration class — prevents the "cannot run inside a transaction block" failure that would abort the deploy (Pitfall 1). |
| T-09-02-02 | Tampering | statusBeforeSuspension / audit columns | mitigate | Explicit `name:` on every new camelCase @Column prevents TypeORM sync from creating shadow columns that could diverge from the migration (Pitfall 2). |
| T-09-02-03 | Tampering / DoS | event_audit_log.note | mitigate | varchar(2000) at DB level + @MaxLength(2000) on AdminEventModerationDto (SEC-01). |
| T-09-02-04 | Repudiation | event_audit_log.adminUserId | mitigate | FK to users(id) ON DELETE SET NULL preserves the audit row while keeping referential integrity. |
| T-09-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (09-RESEARCH.md Package Legitimacy Audit: none). |
</threat_model>

<verification>
- `npm run build` passes (entities + DTOs + migrations compile).
- Migration 1751000000000 declares `public readonly transaction = false`.
- Every new camelCase column declares an explicit `name:`.
- Migrations are written but NOT executed (execution gated to 09-04).
</verification>

<success_criteria>
- EventStatus.SUSPENDED and statusBeforeSuspension exist on the entity (D-01, D-02, D-03).
- EventAuditLogEntity + event_audit_log migration exist (D-13).
- organizer_audit_log gains a nullable adminUserId column in migration (D-12).
- AdminEventQueryDto / AdminEventModerationDto / PaginatedAdminEventsResponseDto compile (D-06–D-09, D-14).
</success_criteria>

<output>
Create `.planning/phases/09-admin-moderation/09-02-SUMMARY.md` when done.
</output>
