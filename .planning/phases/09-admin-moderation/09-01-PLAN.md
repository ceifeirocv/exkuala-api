---
phase: 09-admin-moderation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/organizers/organizer-audit-log.entity.ts
  - src/organizers/dto/organizer-pagination-query.dto.ts
  - src/organizers/dto/paginated-organizers-response.dto.ts
  - src/organizers/organizers.service.ts
  - src/organizers/organizers.service.spec.ts
  - src/organizers/admin-organizers.controller.ts
  - src/organizers/admin-organizers.controller.spec.ts
autonomous: true
requirements: [ADMIN-01, ADMIN-03]
must_haves:
  truths:
    - "Admin can list organizers with cursor pagination, optionally filtered by status"
    - "GET /admin/organizers with no status returns all statuses; with ?status= returns only that status"
    - "Approve and reject record the acting admin's local user id (adminUserId) in the audit row"
  artifacts:
    - path: "src/organizers/dto/organizer-pagination-query.dto.ts"
      provides: "OrganizerPaginationQueryDto (cursor, limit, status)"
      contains: "OrganizerPaginationQueryDto"
    - path: "src/organizers/dto/paginated-organizers-response.dto.ts"
      provides: "PaginatedOrganizersResponseDto wrapping OrganizerEntity[]"
      contains: "PaginatedOrganizersResponseDto"
    - path: "src/organizers/organizers.service.ts"
      provides: "findByStatusPaginated(); approve/reject accept adminUserId"
      contains: "findByStatusPaginated"
    - path: "src/organizers/organizer-audit-log.entity.ts"
      provides: "adminUserId nullable column"
      contains: "adminUserId"
  key_links:
    - from: "src/organizers/admin-organizers.controller.ts"
      to: "OrganizersService.findByStatusPaginated"
      via: "controller delegates to service"
      pattern: "findByStatusPaginated"
    - from: "src/organizers/admin-organizers.controller.ts"
      to: "OrganizersService.approve/reject"
      via: "passes @CurrentUser().id as adminUserId"
      pattern: "user\\.id"
---

<objective>
Upgrade the existing organizer admin surface (Phase 5) to cursor pagination and record the acting admin on every approve/reject. Satisfies ADMIN-01 (list organizers filtered by status, now paginated per D-10) and the audit-integrity half of ADMIN-03 (approve/reject gain adminUserId per D-12). The approve/reject endpoints themselves already exist (D-11) — this plan extends their signatures only.

Purpose: Consistent cursor pagination across both admin lists, plus a non-repudiation trail (which admin acted) for organizer moderation.
Output: Paginated `GET /api/v1/admin/organizers`, new pagination DTOs, `adminUserId` column on `organizer_audit_log`, approve/reject recording the acting admin.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-admin-moderation/09-CONTEXT.md
@.planning/phases/09-admin-moderation/09-PATTERNS.md

# Files extended by this plan (read before editing)
@src/organizers/organizers.service.ts
@src/organizers/admin-organizers.controller.ts
@src/organizers/organizer-audit-log.entity.ts
@src/organizers/organizers.service.spec.ts
@src/organizers/admin-organizers.controller.spec.ts
@src/events/dto/event-pagination-query.dto.ts
@src/events/dto/paginated-events-response.dto.ts

# CRITICAL FACT (overrides 09-PATTERNS.md): AuthenticatedUser has NO `sub` field.
# The local UserEntity.id is `user.id` (see src/types/auth.ts). The audit FK adminUserId → users.id
# MUST be set from `user.id`, NOT `user.sub`. Existing organizers.controller.ts uses user.id for apply().
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — extend specs for paginated list + adminUserId audit</name>
  <files>src/organizers/organizers.service.spec.ts, src/organizers/admin-organizers.controller.spec.ts</files>
  <read_first>
    src/organizers/organizers.service.spec.ts (existing TestingModule + getRepositoryToken setup),
    src/organizers/admin-organizers.controller.spec.ts (existing direct-instantiation setup),
    09-PATTERNS.md "organizers.service.ts" section for findByStatusPaginated shape
  </read_first>
  <behavior>
    Service spec (TestingModule + getRepositoryToken):
    - findByStatusPaginated returns `{ data, nextCursor, hasMore }`; queries createQueryBuilder ordered by (createdAt, id)
    - findByStatusPaginated with status filter adds the status where-clause
    - findByStatusPaginated with no status returns all statuses (no status where-clause)
    - approve(id, adminUserId, note) writes an audit row whose adminUserId equals the passed id
    - reject(id, adminUserId, note) writes an audit row whose adminUserId equals the passed id
    Controller spec (direct instantiation, mocked OrganizersService):
    - GET / delegates to findByStatusPaginated with the query DTO
    - approve passes user.id (NOT user.sub) as the adminUserId argument
    - reject passes user.id as the adminUserId argument
    Stubs import OrganizerPaginationQueryDto and PaginatedOrganizersResponseDto at import level so the
    suite is RED (module-not-found) until Task 2 creates them — honors the two-wave RED→GREEN contract.
  </behavior>
  <action>
    Add the failing test cases above to the two existing spec files. Import the not-yet-existing DTOs
    at the top of each spec so the import itself fails (project RED-stub convention: import non-existent
    source at import level, not assertion level). Update the controller spec's mocked OrganizersService
    to include findByStatusPaginated. Use a named fake repository/service class for mocks per CLAUDE.md
    (no inline stubs). Assert approve/reject receive user.id, never user.sub.
  </action>
  <verify>
    <automated>npm test -- --testPathPatterns=organizers 2>&1 | grep -qE "Cannot find module|FAIL" &amp;&amp; echo RED-OK</automated>
  </verify>
  <done>Both specs fail to compile/import because the new DTOs and method do not yet exist (expected RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — adminUserId column, pagination DTOs, paginated service method</name>
  <files>src/organizers/organizer-audit-log.entity.ts, src/organizers/dto/organizer-pagination-query.dto.ts, src/organizers/dto/paginated-organizers-response.dto.ts, src/organizers/organizers.service.ts</files>
  <read_first>
    src/events/dto/event-pagination-query.dto.ts (DTO analog),
    src/events/dto/paginated-events-response.dto.ts (response wrapper analog),
    src/organizers/organizers.service.ts lines 80-150 (approve/reject/findByStatus to modify),
    09-PATTERNS.md sections for the two DTOs and findByStatusPaginated
  </read_first>
  <behavior>
    - OrganizerPaginationQueryDto exposes cursor?: string, limit?: number (1..100), status?: OrganizerStatus, all @IsOptional with class-validator + @ApiPropertyOptional + @Type(() => Number) on limit
    - PaginatedOrganizersResponseDto = { data: OrganizerEntity[], nextCursor: string | null, hasMore: boolean } with @ApiProperty
    - findByStatusPaginated keyset is (createdAt, id) ASC (organizers have no startAt — RESEARCH Pitfall 6), fetches limit+1, slices, encodes nextCursor from last item's createdAt+id
    - approve/reject append adminUserId to the audit row via create()+save() (never insert())
  </behavior>
  <action>
    Add a nullable `adminUserId` column to OrganizerAuditLogEntity after `note`: `@Column({ type: 'varchar', length: 30, nullable: true, name: 'adminUserId' })` with `@ApiPropertyOptional({ nullable: true })`, typed `string | null` (nullable so pre-Phase-9 rows need no backfill, per D-12 / RESEARCH open question 2). Apply the Phase 7 `name:` convention to prevent sync drift.
    Create OrganizerPaginationQueryDto and PaginatedOrganizersResponseDto mirroring the event DTO analogs (per D-10, D-08-equivalent: admin list returns full entity, not public DTO).
    In OrganizersService: add `findByStatusPaginated(query: OrganizerPaginationQueryDto): Promise<PaginatedOrganizersResponseDto>` using a createQueryBuilder keyset on (createdAt, id) — copy the findOwned shape but adapt the cursor field to createdAt. Add private static encodeCursor(createdAt, id)/decodeCursor mirroring EventsService. Change `approve(id, note?)` → `approve(id: string, adminUserId: string, note?: string)` and `reject(...)` likewise; set `adminUserId` in the create() payload. Keep each method ≤20 lines (extract a cursor helper if needed) per CLAUDE.md. Leave the existing organizer-facing assertTransitionAllowed untouched.
  </action>
  <verify>
    <automated>npm test -- --testPathPatterns=organizers.service</automated>
  </verify>
  <done>organizers.service spec is green; findByStatusPaginated returns the paginated shape; approve/reject persist adminUserId.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — paginate admin controller + pass acting admin id</name>
  <files>src/organizers/admin-organizers.controller.ts</files>
  <read_first>
    src/organizers/admin-organizers.controller.ts (full — current findAll/approve/reject),
    src/organizers/organizers.controller.ts lines 1-40 (correct @CurrentUser().id usage),
    09-PATTERNS.md "admin-organizers.controller.ts (extend)" section
  </read_first>
  <behavior>
    - GET /admin/organizers accepts OrganizerPaginationQueryDto and returns PaginatedOrganizersResponseDto
    - approve/reject resolve the acting admin via @CurrentUser() and pass user.id as adminUserId
  </behavior>
  <action>
    Replace findAll's inline `{ status? }` query type with `@Query() query: OrganizerPaginationQueryDto`, return `findByStatusPaginated(query)`, and update @ApiResponse to `type: PaginatedOrganizersResponseDto` (drop isArray). Add `@CurrentUser() user: AuthenticatedUser` to approve and reject and pass `user.id` (NOT user.sub — see context note; mirror organizers.controller.ts). Import CurrentUser and AuthenticatedUser. Keep @Roles('admin') on every route unchanged (D-16).
  </action>
  <verify>
    <automated>npm test -- --testPathPatterns=admin-organizers.controller</automated>
  </verify>
  <done>admin-organizers.controller spec green; list returns paginated shape; approve/reject forward user.id.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (admin JWT) → AdminOrganizersController | Untrusted role claim must be enforced before any handler runs |
| service → organizer_audit_log | Audit rows must faithfully record the acting admin (non-repudiation) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Elevation of Privilege | GET/PATCH /admin/organizers | mitigate | `@Roles('admin')` on every route (D-16); global RolesGuard returns 403 for user/organizer tokens. Verified by existing admin-organizers guard tests. |
| T-09-01-02 | Repudiation | approve/reject audit row | mitigate | adminUserId set from `@CurrentUser().id` and persisted via create()+save() (D-12); audit row is immutable (no UpdateDateColumn). |
| T-09-01-03 | Spoofing | adminUserId source | mitigate | adminUserId derived server-side from the verified JWT-backed AuthenticatedUser.id, never from the request body. |
| T-09-01-04 | Tampering | OrganizerPaginationQueryDto.limit | mitigate | @Min(1)@Max(100) + service clamps to Math.min(limit,100); prevents unbounded result sets. |
</threat_model>

<verification>
- `npm test -- --testPathPatterns=organizers` is green (service + admin controller).
- adminUserId is nullable in the entity (migration runs in 09-04; no backfill required).
- No change to organizer-facing assertTransitionAllowed (D-05 separation honored).
</verification>

<success_criteria>
- ADMIN-01: `GET /api/v1/admin/organizers` is cursor-paginated, status-filterable, returns full OrganizerEntity items.
- ADMIN-03 (audit half): approve/reject record adminUserId = acting admin's UserEntity.id.
- All organizer specs green; no regressions in existing organizer tests.
</success_criteria>

<output>
Create `.planning/phases/09-admin-moderation/09-01-SUMMARY.md` when done.
</output>
