---
phase: 09-admin-moderation
plan: 04
type: execute
wave: 3
depends_on: ["09-01", "09-02", "09-03"]
files_modified: []
autonomous: false
requirements: [EVT-03, ADMIN-01, ADMIN-02, ADMIN-03, ADMIN-04]
must_haves:
  truths:
    - "Both Phase 9 migrations run cleanly against Neon"
    - "event_audit_log table and organizer_audit_log.adminUserId column exist in the live DB"
    - "event_status enum contains SUSPENDED in the live DB; events.statusBeforeSuspension column exists"
    - "Full Jest suite is green after migrations"
    - "Admin can list/suspend/restore/remove events and list/approve/reject organizers end-to-end (human verified)"
  artifacts: []
  key_links:
    - from: "src/database/migrations/1751000000000-admin-event-status.ts"
      to: "Neon event_status enum + events table"
      via: "pnpm run migration:run"
      pattern: "SUSPENDED"
    - from: "src/database/migrations/1751000000001-admin-audit-log.ts"
      to: "Neon event_audit_log + organizer_audit_log"
      via: "pnpm run migration:run"
      pattern: "event_audit_log"
---

<objective>
[BLOCKING] Run the Phase 9 migrations against Neon, prove the full test suite is green against the migrated schema, and human-verify the admin moderation surfaces end-to-end. Build + type checks pass without this step (types come from entity files, not the live DB), so this gate is mandatory to avoid a false-positive verification state (project memory: typeorm-synchronize-constraint-drift).

Purpose: Close the schema-push gap and confirm both admin surfaces work against the real database.
Output: Migrated Neon schema, green suite, human sign-off.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-admin-moderation/09-RESEARCH.md

# 09-RESEARCH Pitfall 1: enum migration must have transaction=false (verified in 09-02).
# 09-RESEARCH Pitfall 5: organizer_audit_log adminUserId add may hit Neon constraint drift — verify post-run.
# migration:run builds to dist/ then runs against dist/database/data-source.js (package.json).
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Run migrations against Neon and verify schema</name>
  <files></files>
  <read_first>
    src/database/migrations/1751000000000-admin-event-status.ts,
    src/database/migrations/1751000000001-admin-audit-log.ts,
    package.json migration:run script
  </read_first>
  <action>
    Run `pnpm run migration:run` (builds to dist/ then applies both Phase 9 migrations against Neon). If the enum-extension migration errors with "ALTER TYPE ... ADD VALUE cannot run inside a transaction block" (PG 25001), the transaction=false flag is missing — fix in 09-02's migration file and rerun. If the organizer_audit_log adminUserId ALTER fails with "column already exists" or a constraint error, inspect Neon for prior synchronize drift (project memory typeorm-synchronize-constraint-drift) and reconcile via the IF NOT EXISTS guard before rerunning. After success, confirm the live schema: event_status enum contains SUSPENDED, events has statusBeforeSuspension, event_audit_log table + its two indexes exist, organizer_audit_log has adminUserId.
  </action>
  <verify>
    <automated>pnpm run migration:run 2>&1 | tee /tmp/09-migrate.log; grep -qiE "migration .*has been executed successfully|No migrations are pending" /tmp/09-migrate.log &amp;&amp; ! grep -qiE "cannot run inside a transaction|QueryFailedError" /tmp/09-migrate.log &amp;&amp; echo MIGRATE-OK</automated>
  </verify>
  <done>Both migrations applied; no transaction-block or QueryFailed errors; SUSPENDED, statusBeforeSuspension, event_audit_log, and organizer_audit_log.adminUserId present in Neon.</done>
</task>

<task type="auto">
  <name>Task 2: Full test suite green against migrated schema</name>
  <files></files>
  <read_first>package.json jest config</read_first>
  <action>
    Run the full Jest suite (`npm test`). All Phase 9 specs (organizers, admin-organizers.controller, admin-events.service, admin-events.controller, events.service) plus all prior-phase specs must pass. Investigate and fix any regression before sign-off — do not skip or mark pending.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -20 | grep -qE "Tests:.*[0-9]+ passed" &amp;&amp; ! npm test 2>&1 | grep -qE "failed|FAIL" &amp;&amp; echo SUITE-GREEN</automated>
  </verify>
  <done>Full Jest suite passes with zero failures.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Admin moderation surfaces: paginated GET /api/v1/admin/organizers with adminUserId on approve/reject;
    GET /api/v1/admin/events (all statuses/owners, ?status=, ?organizerId=, ?includeDeleted=true);
    PATCH /admin/events/:id/suspend, PATCH /admin/events/:id/restore, DELETE /admin/events/:id; each writing an event_audit_log row.
  </what-built>
  <how-to-verify>
    1. Start the API (`pnpm run start:dev`) and open Swagger at /api/docs — confirm "Admin - Events" and "Admin - Organizers" tags with the new routes.
    2. With an ADMIN JWT:
       a. `GET /api/v1/admin/organizers` → paginated shape `{ data, nextCursor, hasMore }`; try `?status=pending`.
       b. `GET /api/v1/admin/events` → returns events of ALL statuses incl. drafts; full entity fields (status, organizerId, statusBeforeSuspension) present.
       c. Pick a PUBLISHED event: `PATCH /admin/events/:id/suspend` (204) → re-GET shows status SUSPENDED + statusBeforeSuspension=PUBLISHED.
       d. `PATCH /admin/events/:id/restore` (204) → status back to PUBLISHED, statusBeforeSuspension null.
       e. `DELETE /admin/events/:id` with note (204) → event gone from GET /admin/events default, reappears with `?includeDeleted=true` showing deletedAt.
       f. `PATCH /admin/organizers/:id/approve` → check event_audit_log/organizer_audit_log rows carry adminUserId.
    3. With a NON-ADMIN (user/organizer) JWT: every /admin/* route returns 403.
    4. As the owning organizer: `PATCH /api/v1/events/:id` on a SUSPENDED event returns 409.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration runner → Neon | DDL applied at deploy time; must not corrupt existing schema |
| live admin endpoints → non-admin clients | 403 enforcement verified against the running app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | Denial of Service | migration:run on Neon | mitigate | transaction=false verified pre-run; migration log checked for transaction-block error; rollback path documented in migration down(). |
| T-09-04-02 | Elevation of Privilege | live /admin/* routes | mitigate | Human verification step 3 confirms 403 for non-admin tokens end-to-end. |
| T-09-04-03 | Repudiation | audit rows in live DB | mitigate | Human verification step 2f confirms adminUserId populated on real moderation actions. |
</threat_model>

<verification>
- `pnpm run migration:run` succeeds with no transaction-block/QueryFailed errors.
- `npm test` full suite green.
- Human checkpoint approved (admin actions, 403 for non-admin, 409 organizer-on-SUSPENDED, audit rows populated).
</verification>

<success_criteria>
- All Phase 9 requirements (EVT-03, ADMIN-01, ADMIN-02, ADMIN-03, ADMIN-04) demonstrably satisfied against the live Neon schema.
- Migrations applied; full suite green; human sign-off recorded.
</success_criteria>

<output>
Create `.planning/phases/09-admin-moderation/09-04-SUMMARY.md` when done.
</output>
