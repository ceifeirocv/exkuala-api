---
phase: 02.1-add-a-webhook-endpoint-for-auth0-to-add-or-refresh-user-on-c
plan: 01
subsystem: testing
tags: [jest, nestjs, webhooks, auth0, typeorm, tdd]

# Dependency graph
requires:
  - phase: 02-auth-infrastructure
    provides: JwtAuthGuard, OptionalJwtAuthGuard, UserEntity with auth0Id column

provides:
  - Failing test stubs for WebhookSecretGuard (4 tests, RED)
  - Failing test stubs for UsersService.upsertFromAuth0 (3 tests, RED)
  - Failing test stub for WebhookController delegation (1 test, RED)

affects:
  - 02.1-02 (UsersService implementation — turns users.service.spec RED tests GREEN)
  - 02.1-03 (WebhookSecretGuard implementation — turns webhook-secret.guard.spec RED tests GREEN)
  - 02.1-04 (WebhookController implementation — turns webhooks.controller.spec RED test GREEN)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "RED test stub pattern: import non-existent source module to guarantee test suite failure at import level"
    - "mockContext factory: function(headerValue: string | undefined) for guard specs"
    - "TestingModule + getRepositoryToken pattern for TypeORM service specs"
    - "Direct instantiation pattern for controller specs (no TestingModule)"

key-files:
  created:
    - src/webhooks/guards/webhook-secret.guard.spec.ts
    - src/users/users.service.spec.ts
    - src/webhooks/webhooks.controller.spec.ts
  modified: []

key-decisions:
  - "Wave 0 test stubs import non-existent source files to guarantee RED at import level, not assertion level — ensures tests are live, not no-ops"
  - "Controller spec uses direct instantiation (not TestingModule) to keep it minimal — mirrors jwt-auth.guard.spec.ts pattern"
  - "users.service.spec.ts uses TestingModule + getRepositoryToken to exercise NestJS DI wiring from day one"

patterns-established:
  - "Guard specs: mockContext(headerValue: string | undefined) with switchToHttp headers pattern"
  - "Service specs: TestingModule + getRepositoryToken(Entity) + mockRepository const outside describe"
  - "Controller specs: direct instantiation with mockService as unknown as ServiceType"

requirements-completed:
  - AUTH-03

# Metrics
duration: 4min
completed: 2026-05-02
---

# Phase 02.1 Plan 01: Wave 0 RED Test Stubs Summary

**Three failing test stubs for WebhookSecretGuard (4 tests), UsersService.upsertFromAuth0 (3 tests), and WebhookController (1 test) — all import non-existent source files to guarantee RED state before Wave 1/2 implementation.**

## Performance

- **Duration:** 4 min
- **Started:** 2026-05-02T18:22:23Z
- **Completed:** 2026-05-02T18:26:29Z
- **Tasks:** 3
- **Files modified:** 3

## Accomplishments

- Created `src/webhooks/guards/webhook-secret.guard.spec.ts` with 4 test cases covering: valid secret match, wrong secret, length-mismatch (no throw), missing header
- Created `src/users/users.service.spec.ts` with 3 test cases covering: upsert args with auth0Id, id generation, idempotent second call
- Created `src/webhooks/webhooks.controller.spec.ts` with 1 test case covering: delegation of handleAuth0Webhook to usersService.upsertFromAuth0
- All 3 spec files fail RED: guard and controller specs fail at import (module not found); service spec fails at UserEntity import (ESM issue in user.entity.ts — pre-existing)

## Task Commits

1. **Task 1: Create webhook-secret.guard.spec.ts stub** - `0134599` (test)
2. **Task 2: Create users.service.spec.ts stub** - `c7fc8a6` (test)
3. **Task 3: Create webhooks.controller.spec.ts stub** - `5ea8da8` (test)

## Files Created/Modified

- `src/webhooks/guards/webhook-secret.guard.spec.ts` - Failing unit tests for WebhookSecretGuard (4 cases)
- `src/users/users.service.spec.ts` - Failing unit tests for UsersService.upsertFromAuth0 (3 cases)
- `src/webhooks/webhooks.controller.spec.ts` - Failing unit test for WebhookController delegation (1 case)

## Decisions Made

- Controller spec uses direct instantiation (no TestingModule) to match the minimal pattern from `jwt-auth.guard.spec.ts` — keeps the spec focused on delegation behavior
- Service spec uses TestingModule + getRepositoryToken so the DI wiring is tested from the first GREEN pass, not added as an afterthought
- `mockRepository` is declared as a `const` outside the `describe` block (matching the RESEARCH.md pattern) so `mock.calls` accumulate across `it` blocks within the same test run — `jest.clearAllMocks()` in `beforeEach` resets between tests

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- Jest 30 replaced `--testPathPattern` with `--testPathPatterns` (plural). The plan's verification command used the old flag name. Used `npx jest --testPathPatterns=...` directly to run verification. The `pnpm test -- --testPathPattern=...` form in the plan fails because pnpm passes the argument as a literal test pattern, not a flag — this is an existing project tooling quirk unrelated to Wave 0 content.
- `users.service.spec.ts` also triggers the pre-existing `@paralleldrive/cuid2` ESM transform issue (visible in `user.entity.spec.ts` as well). The spec still exits non-zero (RED) as required; this is not caused by Wave 0 and will be resolved when `transformIgnorePatterns` is corrected.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- RED state established for all three spec files
- Wave 1 plans (02.1-02 UsersService, 02.1-03 WebhookSecretGuard) can execute in parallel
- Wave 2 plan (02.1-04 WebhookController) depends on Wave 1 completion
- Pre-existing ESM issue with `@paralleldrive/cuid2` in jest `transformIgnorePatterns` should be addressed in Wave 1 when UsersService is implemented (service test will need this fixed to go GREEN)

---
*Phase: 02.1-add-a-webhook-endpoint-for-auth0-to-add-or-refresh-user-on-c*
*Completed: 2026-05-02*
