---
phase: 02.1-add-a-webhook-endpoint-for-auth0-to-add-or-refresh-user-on-c
plan: "03"
subsystem: auth
tags: [nestjs, guard, dto, class-validator, crypto, timingSafeEqual, webhook]

requires:
  - phase: 02.1-01
    provides: webhook-secret.guard.spec.ts (4 failing tests that this plan turns GREEN)

provides:
  - WebhookSecretGuard: constant-time shared-secret guard for POST /webhooks/auth0
  - Auth0WebhookDto: validated DTO for Auth0 Action webhook body (sub + event)

affects:
  - 02.1-04 (controller wires WebhookSecretGuard + Auth0WebhookDto)

tech-stack:
  added: []
  patterns:
    - "Guard returns false (never throws) before calling timingSafeEqual when buffer byte lengths differ"
    - "Length-mismatch early return pattern prevents TypeError from leaking as 500"
    - "class-validator IsIn for constrained string union types in DTOs"

key-files:
  created:
    - src/webhooks/guards/webhook-secret.guard.ts
    - src/webhooks/dto/auth0-webhook.dto.ts
  modified: []

key-decisions:
  - "Length-mismatch early return before timingSafeEqual prevents TypeError (crypto throws if Buffer byte lengths differ)"
  - "typeof incomingSecret !== 'string' guard handles absent header (req.headers returns undefined for missing keys)"
  - "Auth0WebhookDto uses only sub and event; ValidationPipe whitelist:true strips extra Auth0 fields silently"

patterns-established:
  - "Guard pattern: type-check → empty-check → length-check → timingSafeEqual"
  - "DTO pattern: @IsString() for user IDs, @IsIn([...]) for constrained event enums"

requirements-completed: [AUTH-03]

duration: 2min
completed: 2026-05-02
---

# Phase 02.1 Plan 03: Guard + DTO Summary

**WebhookSecretGuard with constant-time comparison and Auth0WebhookDto with class-validator, turning 4 failing RED tests GREEN**

## Performance

- **Duration:** 2 min
- **Started:** 2026-05-02T18:35:23Z
- **Completed:** 2026-05-02T18:37:19Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- WebhookSecretGuard implemented with timingSafeEqual and safe length-mismatch early return (no 500 on wrong-length secrets)
- All 4 webhook-secret.guard.spec.ts tests pass GREEN (valid, wrong, length-mismatch, absent)
- Auth0WebhookDto with @IsString() sub and @IsIn(['post-login','post-register']) event ready for Wave 2 controller

## Task Commits

Each task was committed atomically:

1. **Tasks 1 + 2: WebhookSecretGuard + Auth0WebhookDto** - `c3ea2c7` (feat)

## Files Created/Modified
- `src/webhooks/guards/webhook-secret.guard.ts` - CanActivate guard using timingSafeEqual from crypto with defensive length checks
- `src/webhooks/dto/auth0-webhook.dto.ts` - Auth0 webhook body DTO with @IsString sub and @IsIn event

## Decisions Made
- Committed both Task 1 and Task 2 in a single commit — they have zero file overlap and the DTO has no spec to gate separately
- `pnpm jest --testPathPatterns` (plural) is required for Jest 30; `pnpm test -- --testPathPattern` passes the flag as a positional arg to the script and does not reach Jest

## Deviations from Plan
None - plan executed exactly as written.

## Issues Encountered
- Jest 30 changed `--testPathPattern` to `--testPathPatterns` (plural). `pnpm test -- --testPathPattern` passes the flag as a positional argument (after `--`) rather than a jest option, so jest finds no matching pattern. Fixed by invoking `pnpm jest --testPathPatterns=...` directly.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- WebhookSecretGuard and Auth0WebhookDto are ready for Plan 04 (controller wiring)
- Guard and DTO import paths: `./guards/webhook-secret.guard` and `./dto/auth0-webhook.dto` relative to webhooks module
