---
phase: 06-organizer-event-crud
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/events/events.service.spec.ts
  - src/events/events.controller.spec.ts
autonomous: true
requirements:
  - ORG-04
  - ORG-05
  - EVT-01
  - EVT-02
  - EVT-05

must_haves:
  truths:
    - "events.service.spec.ts imports EventsService from a path that does not yet exist — compile-fail is expected and intentional"
    - "events.controller.spec.ts imports EventsController from a path that does not yet exist — compile-fail is expected and intentional"
    - "All described test cases enumerate the full service and controller contract so Wave 2 and Wave 3 implementation has a deterministic target"
    - "Running `pnpm test` after Wave 0 fails on the two new spec files and passes on all pre-existing specs"
  artifacts:
    - path: "src/events/events.service.spec.ts"
      provides: "RED stubs covering create, findOwned (cursor pagination), findOwnedById, update (field patch + status transition + publish gate), softDeleteDraft"
    - path: "src/events/events.controller.spec.ts"
      provides: "RED stubs covering all 5 controller endpoints: POST, GET list, GET :id, PATCH, DELETE"
  key_links:
    - from: "src/events/events.service.spec.ts"
      to: "src/events/events.service.ts"
      via: "import { EventsService } from './events.service'"
      pattern: "from './events.service'"
    - from: "src/events/events.controller.spec.ts"
      to: "src/events/events.controller.ts"
      via: "import { EventsController } from './events.controller'"
      pattern: "from './events.controller'"
---

<objective>
Write Wave 0 TDD RED stubs — spec files that import from source files that do not yet exist. These specs define the full contract EventsService and EventsController must satisfy. Compile failure on the two new files is intentional and expected.

Purpose: Force failing tests to exist before any source is written. Wave 2 (service) and Wave 3 (controller) turn these RED specs GREEN. This is the only Wave 0 deliverable — no implementation files are created here.

Output:
- src/events/events.service.spec.ts (RED — imports non-existent EventsService)
- src/events/events.controller.spec.ts (RED — imports non-existent EventsController)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-organizer-event-crud/06-CONTEXT.md

<!-- Key interfaces the spec files import against (will not exist until Wave 1/2) -->
<interfaces>
<!-- EventStatus enum — already in src/events/event.entity.ts -->
export enum EventStatus {
  DRAFT = 'DRAFT',
  PUBLISHED = 'PUBLISHED',
  CANCELLED = 'CANCELLED',
}

<!-- EventEntity shape (existing, Wave 1 adds @ManyToOne relations) -->
export class EventEntity {
  id: string;
  title: string;
  description: string | null;
  startAt: Date;
  endAt: Date | null;
  venueName: string | null;
  address: string | null;
  categoryId: string | null;
  ticketPrice: number | null;
  externalTicketUrl: string | null;
  status: EventStatus;
  organizerId: string | null;  // becomes NOT NULL after migration
  deletedAt: Date | null;
  createdAt: Date;
  updatedAt: Date;
}

<!-- OrganizerEntity — from src/organizers/organizer.entity.ts -->
export class OrganizerEntity {
  id: string;
  userId: string;
  name: string;
  status: OrganizerStatus;
  // ... other fields
}

<!-- Target service interface (to be implemented in Wave 2) -->
// src/events/events.service.ts
export class EventsService {
  create(organizerId: string, dto: CreateEventDto): Promise<EventEntity>
  findOwned(organizerId: string, query: EventPaginationQueryDto): Promise<PaginatedEventsResponseDto>
  findOwnedById(organizerId: string, eventId: string): Promise<EventEntity>
  update(organizerId: string, eventId: string, dto: UpdateEventDto): Promise<EventEntity>
  softDeleteDraft(organizerId: string, eventId: string): Promise<void>
  toResponseDto(event: EventEntity): EventResponseDto
}

<!-- Target controller interface (to be implemented in Wave 3) -->
// src/events/events.controller.ts — routes under /organizer/events
// POST   /organizer/events          → create
// GET    /organizer/events          → findOwned
// GET    /organizer/events/:id      → findOwnedById
// PATCH  /organizer/events/:id      → update
// DELETE /organizer/events/:id      → softDeleteDraft

<!-- DTO shapes (to be created in Wave 1, referenced in specs as any/partial) -->
// CreateEventDto   — title (required), startAt (required), categoryId (required), plus optionals
// UpdateEventDto   — all fields optional; status triggers state machine
// EventResponseDto — manual mapping from entity; no deletedAt
// PaginatedEventsResponseDto — { data: EventResponseDto[], nextCursor: string | null, hasMore: boolean }
// EventPaginationQueryDto — cursor?, limit?, status?
</interfaces>

Prior phase patterns to mirror:
@src/organizers/organizers.service.spec.ts  (TestingModule + getRepositoryToken pattern)
@src/organizers/organizers.controller.spec.ts  (direct instantiation + named mock service)
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Write RED spec — events.service.spec.ts</name>
  <files>src/events/events.service.spec.ts</files>
  <behavior>
    - describe('EventsService') wraps all groups
    - beforeEach: TestingModule with EventsService + getRepositoryToken(EventEntity) mock; follows organizers.service.spec.ts pattern exactly
    - Named mock repository: const mockEventRepository = { findOne: jest.fn(), save: jest.fn(), create: jest.fn(), find: jest.fn(), softDelete: jest.fn(), createQueryBuilder: jest.fn() }
    - createQueryBuilder mock returns a chainable builder stub: { where: jest.fn().mockReturnThis(), andWhere: jest.fn().mockReturnThis(), orderBy: jest.fn().mockReturnThis(), addOrderBy: jest.fn().mockReturnThis(), take: jest.fn().mockReturnThis(), getMany: jest.fn() }

    Test group: create() (EVT-01, ORG-04)
    - 'creates a draft event and returns it' — dto with title/startAt/categoryId; mock create+save; assert status=DRAFT, organizerId set
    - 'creates event with all optional fields' — full payload including description/venueName/address/ticketPrice/externalTicketUrl/endAt

    Test group: findOwned() — cursor pagination (D-17, D-18, D-19, D-20, EVT-06)
    - 'returns first page with hasMore=false when results ≤ limit'
    - 'returns first page with hasMore=true and nextCursor when results > limit'
    - 'accepts cursor param and applies row-value WHERE clause'
    - 'filters by status when status query param provided'
    - 'returns empty data array when organizer has no events'

    Test group: findOwnedById() (D-21, ORG-05)
    - 'returns event when owned by organizer'
    - 'throws NotFoundException when event not found' — mock returns null
    - 'throws NotFoundException (not ForbiddenException) when event belongs to different organizer' — mock returns null (compound WHERE)

    Test group: update() — field patch (D-05, ORG-04, ORG-05)
    - 'updates allowed fields on a draft event'
    - 'updates allowed fields on a published event (D-05)'
    - 'throws ConflictException when patching a cancelled event (D-05)'
    - 'throws NotFoundException for non-owned event'

    Test group: update() — status transitions (D-02, D-03, D-04, D-07)
    - 'transitions draft → published when publish gate passes'
    - 'throws UnprocessableEntityException with missing fields when publish gate fails (D-10)'
    - 'throws UnprocessableEntityException when startAt is in the past (D-06)'
    - 'transitions published → cancelled (D-04)'
    - 'throws ConflictException on draft → cancelled (D-04: drafts are deleted, not cancelled)'
    - 'throws ConflictException on published → draft (D-02: no reverse transitions)'
    - 'throws ConflictException on cancelled → published (D-03: terminal state)'

    Test group: softDeleteDraft() (EVT-05, D-15)
    - 'calls repository.softDelete() for owned draft event'
    - 'throws ConflictException when event status is published (D-15)'
    - 'throws ConflictException when event status is cancelled (D-15)'
    - 'throws NotFoundException for non-owned event'
  </behavior>
  <action>
    Create src/events/events.service.spec.ts.

    Import structure at top of file (these imports WILL fail at compile time — intentional RED state):
    ```
    import { EventsService } from './events.service';
    ```
    Also import from existing paths that DO exist:
    ```
    import { Test, TestingModule } from '@nestjs/testing';
    import { getRepositoryToken } from '@nestjs/typeorm';
    import { ConflictException, NotFoundException, UnprocessableEntityException } from '@nestjs/common';
    import { EventEntity, EventStatus } from './event.entity';
    ```

    Structure the file as describe('EventsService') with the test groups and cases listed in the behavior block above. Each test case body may use expect(true).toBe(true) as a placeholder — the RED state comes from the broken import, not from assertion logic. The cases must exist so Wave 2 can fill in real assertions.

    Follow the named-mock pattern from organizers.service.spec.ts exactly. jest.clearAllMocks() in beforeEach.

    The queryBuilder mock must handle chaining: every method returns mockReturnThis() except getMany which returns mockResolvedValue([]).

    Comment at top of file: "Wave 0 RED stubs — EventsService is not yet implemented. This file intentionally fails to compile until Wave 2 creates events.service.ts."
  </action>
  <verify>
    <automated>cd /home/ceifeiro/Code/exkuala-api && pnpm test --testPathPattern="events.service.spec" 2>&1 | grep -E "FAIL|Cannot find module|error TS" | head -5</automated>
  </verify>
  <done>
    File exists at src/events/events.service.spec.ts. Running the spec produces a compile error referencing "./events.service" (module not found). All described test cases are present as skeletal it() blocks.
  </done>
</task>

<task type="tdd">
  <name>Task 2: Write RED spec — events.controller.spec.ts</name>
  <files>src/events/events.controller.spec.ts</files>
  <behavior>
    - describe('EventsController') wraps all groups
    - beforeEach: direct instantiation pattern (no TestingModule): controller = new EventsController(mockEventsService as unknown as EventsService)
    - Named mock service: const mockEventsService = { create: jest.fn(), findOwned: jest.fn(), findOwnedById: jest.fn(), update: jest.fn(), softDeleteDraft: jest.fn(), toResponseDto: jest.fn((e) => e) }

    Test group: POST /organizer/events (ORG-04, EVT-01)
    - 'returns 201 with EventResponseDto when service.create() succeeds'
    - 'propagates ConflictException from service'

    Test group: GET /organizer/events (D-16, D-17, EVT-06)
    - 'returns paginated events for the organizer'
    - 'passes cursor and status filter to service'

    Test group: GET /organizer/events/:id (D-21, ORG-05)
    - 'returns EventResponseDto for owned event'
    - 'propagates NotFoundException from service'

    Test group: PATCH /organizer/events/:id (ORG-04, ORG-05, EVT-02)
    - 'returns updated EventResponseDto when service.update() succeeds'
    - 'propagates ConflictException (status transition) from service'
    - 'propagates UnprocessableEntityException (publish gate) from service'

    Test group: DELETE /organizer/events/:id (EVT-05, D-15)
    - 'returns 204 when service.softDeleteDraft() succeeds'
    - 'propagates ConflictException when event is not draft'
  </behavior>
  <action>
    Create src/events/events.controller.spec.ts.

    Import structure (the EventsController import WILL fail — intentional RED):
    ```
    import { EventsController } from './events.controller';
    import { EventsService } from './events.service';
    ```
    Also import from existing paths:
    ```
    import { ConflictException, NotFoundException, UnprocessableEntityException } from '@nestjs/common';
    import { EventEntity, EventStatus } from './event.entity';
    import { OrganizerEntity } from '../organizers/organizer.entity';
    ```

    Follow direct-instantiation pattern from organizers.controller.spec.ts. jest.clearAllMocks() in beforeEach.

    Each test case body may use expect(true).toBe(true) as placeholder. The RED state comes from the broken imports.

    Comment at top of file: "Wave 0 RED stubs — EventsController is not yet implemented. This file intentionally fails to compile until Wave 3 creates events.controller.ts."
  </action>
  <verify>
    <automated>cd /home/ceifeiro/Code/exkuala-api && pnpm test --testPathPattern="events.controller.spec" 2>&1 | grep -E "FAIL|Cannot find module|error TS" | head -5</automated>
  </verify>
  <done>
    File exists at src/events/events.controller.spec.ts. Running the spec produces a compile error referencing "./events.controller" (module not found). All described test cases are present as skeletal it() blocks.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test→source | Spec files import from non-existent source paths (intentional compile-fail) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | Spec stubs | accept | Wave 0 specs are test infrastructure only — no production code, no security surface |
</threat_model>

<verification>
After both tasks:
- pnpm test runs and passes all pre-existing tests (25+ passing)
- The two new spec files fail with "Cannot find module './events.service'" and "Cannot find module './events.controller'"
- No other test files are broken
</verification>

<success_criteria>
- src/events/events.service.spec.ts exists with all described test groups and cases
- src/events/events.controller.spec.ts exists with all described test groups and cases
- Pre-existing test suite (25+ tests) continues to pass
- Both new spec files fail to compile (module not found) — this is the expected RED state
</success_criteria>

<output>
After completion, create `.planning/phases/06-organizer-event-crud/06-01-SUMMARY.md`
</output>
