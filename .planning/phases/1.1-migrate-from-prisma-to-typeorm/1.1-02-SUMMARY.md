---
phase: 1.1
plan: "02"
subsystem: dependencies
tags: [typeorm, prisma, migration, package-json]
dependency_graph:
  requires: [1.1-01]
  provides: [typeorm-packages, migration-scripts]
  affects: [package.json, node_modules]
tech_stack:
  added: ["@nestjs/typeorm@^11.0.1", "typeorm@^0.3.28", "@paralleldrive/cuid2@^3.3.0", "dotenv@^17.4.2"]
  patterns: [migration-cli-via-compiled-datasource]
key_files:
  modified: [package.json, package-lock.json]
decisions:
  - "postinstall hook removed before Prisma uninstall to prevent npm hook failure during uninstall"
  - "migration scripts use npm run build first so TypeORM CLI reads compiled dist/database/data-source.js"
metrics:
  duration: "~5 minutes"
  completed: "2026-04-19"
  tasks_completed: 2
  tasks_total: 2
---

# Phase 1.1 Plan 02: Dependency Swap (TypeORM in, Prisma out) Summary

TypeORM stack installed and Prisma packages fully removed; `package.json` now has `migration:generate`, `migration:run`, `migration:revert` scripts pointing at `dist/database/data-source.js`.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Install TypeORM packages and remove Prisma packages | 77fa570 | package.json, package-lock.json |
| 2 | Update package.json scripts (add migration scripts, remove Prisma scripts) | 77fa570 | package.json |

## Verification Results

All acceptance criteria passed:

- `node -e "require('@nestjs/typeorm')"` — exits 0
- `node -e "require('typeorm')"` — exits 0
- `node -e "require('@paralleldrive/cuid2')"` — exits 0
- `node -e "require('dotenv')"` — exits 0
- `@nestjs/typeorm`, `typeorm`, `@paralleldrive/cuid2`, `dotenv` all present in `package.json` dependencies
- `@prisma/adapter-pg`, `@prisma/client`, `prisma` all absent from `package.json`
- `postinstall` hook absent from `package.json`
- `migration:generate`, `migration:run`, `migration:revert` all present pointing at `dist/database/data-source.js`
- `prisma:generate`, `prisma:migrate`, `prisma:studio` all absent
- `package.json` valid JSON

## Test Suite Results

`npm test` — 4 test suites run, 2 passed, 2 failed.

The 2 failures (`event.entity.spec.ts`, `user.entity.spec.ts`) are intentional TDD RED stubs created by plan 1.1-01. They import `event.entity.ts` and `user.entity.ts` which do not exist yet and will be created by plan 1.1-03. These failures are expected and pre-date this plan.

The 2 passing suites (`app.controller.spec.ts`, `app.service.spec.ts` equivalent) remain green.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Trailing comma after postinstall removal**
- **Found during:** Task 1
- **Issue:** After removing the `"postinstall": "prisma generate"` line, the preceding `"prisma:studio"` line retained a trailing comma making the JSON invalid.
- **Fix:** Removed the trailing comma before running `npm install` to ensure npm could parse `package.json`.
- **Files modified:** package.json
- **Commit:** 77fa570

## Known Stubs

None — this plan modifies only `package.json` (dependency manifest), no UI or data-rendering code.

## Threat Flags

None — no new network endpoints, auth paths, file access patterns, or schema changes introduced.

## Self-Check: PASSED

- package.json exists and is valid JSON: CONFIRMED
- Commit 77fa570 exists: CONFIRMED
- `@nestjs/typeorm` in dependencies: CONFIRMED
- `prisma` absent from dependencies: CONFIRMED
- `migration:run` script present: CONFIRMED
- `postinstall` absent: CONFIRMED
