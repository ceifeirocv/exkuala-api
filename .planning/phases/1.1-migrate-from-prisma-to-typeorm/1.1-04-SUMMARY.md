---
phase: "1.1"
plan: "04"
subsystem: "database"
tags: [typeorm, migrations, data-source, baseline, cli]
dependency_graph:
  requires: [1.1-02, 1.1-03]
  provides: [AppDataSource, BaselineMigration]
  affects: [1.1-05, 1.1-06]
tech_stack:
  added: []
  patterns: ["TypeORM CLI DataSource (process.env, no NestJS)", "build-first migration pattern (dist/** globs)", "manual baseline migration squash (D-03)"]
key_files:
  created:
    - src/database/data-source.ts
    - src/database/migrations/1745000000000-baseline.ts
  modified:
    - src/app.module.ts
  deleted:
    - prisma.config.ts
    - src/prisma/prisma.module.ts
    - src/prisma/prisma.service.ts
decisions:
  - "data-source.ts uses dist/**/*.entity.js globs (not src/) — nodenext module prevents TypeScript source loading in CLI"
  - "Baseline migration timestamp 1745000000000 is fixed (D-03 squash) — class name Baseline1745000000000 matches filename"
  - "Prisma blocking files removed early (Rule 3) — Plan 05 will complete TypeOrmModule wiring; app.module.ts left with stub comment"
metrics:
  duration: "~8m"
  completed: "2026-04-19"
  tasks_completed: 2
  files_created: 2
  files_modified: 1
  files_deleted: 3
---

# Phase 1.1 Plan 04: TypeORM CLI DataSource and Baseline Migration Summary

**One-liner:** TypeORM CLI DataSource exporting AppDataSource via process.env with dist/** globs, plus a manually-written baseline migration squashing the full Prisma schema (users + events + event_status enum) into a single TypeORM migration file.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Create src/database/data-source.ts | 1e5c3a3 | src/database/data-source.ts, src/app.module.ts (stub), deleted: prisma.config.ts, src/prisma/prisma.{module,service}.ts |
| 2 | Write baseline migration (D-03 squash) | 58b2b09 | src/database/migrations/1745000000000-baseline.ts |

## Verification Results

- `test -f src/database/data-source.ts` — PASS
- `test -f src/database/migrations/1745000000000-baseline.ts` — PASS
- `npm run build` — PASS (0 errors)
- `grep -c "CREATE TABLE" ...baseline.ts` — 2 (users + events)
- `grep "event_status" ...baseline.ts` — 4 occurrences (CREATE TYPE, DEFAULT clause in events, schema-qualified column type)
- All varchar length grep checks — PASS (30, 128, 200, 500, 5000, 2048, DECIMAL(10,2))
- `npm test` — 14/14 GREEN

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Prisma source files blocking npm run build**
- **Found during:** Task 1 — first build attempt after creating data-source.ts
- **Issue:** `src/prisma/prisma.service.ts` imports `@prisma/adapter-pg` and `../generated/prisma/client`; `prisma.config.ts` imports from `prisma/config` — both packages were removed in Plan 02. These caused 2 TypeScript compile errors (`TS2307: Cannot find module`) blocking all compilation.
- **Fix:** Deleted `src/prisma/prisma.service.ts`, `src/prisma/prisma.module.ts`, `prisma.config.ts`; removed `PrismaModule` import from `src/app.module.ts` (replaced with stub comment "TypeOrmModule.forRootAsync wired in Plan 05").
- **Scope note:** This is a strict subset of Plan 05's scope (which also wires TypeOrmModule and removes prisma/ + src/generated/prisma/). Plan 05 is unaffected — the stub comment guides it.
- **Files modified/deleted:** `prisma.config.ts` (deleted), `src/prisma/prisma.module.ts` (deleted), `src/prisma/prisma.service.ts` (deleted), `src/app.module.ts` (PrismaModule import removed)
- **Commit:** 1e5c3a3

## Known Stubs

- `src/app.module.ts` — `TypeOrmModule.forRootAsync` is not yet wired; stub comment references Plan 05. This is intentional — Plan 05 owns the TypeOrmModule registration. The app does not start with a real database connection until Plan 05 completes.

## Threat Flags

None — all threat mitigations from the plan's threat model are implemented:
- T-1.1-04-01: `synchronize: false` hardcoded in CLI DataSource
- T-1.1-04-02: `migrationsRun: false` left to Plan 05 (AppModule); CLI DataSource has no auto-run
- T-1.1-04-03: DATABASE_URL read from process.env; .env already in .gitignore
- T-1.1-04-04: All varchar lengths verified by grep acceptance criteria (30, 128, 5000, DECIMAL(10,2))

## Self-Check: PASSED

- src/database/data-source.ts — FOUND
- src/database/migrations/1745000000000-baseline.ts — FOUND
- commit 1e5c3a3 — FOUND (feat(1.1-04): create TypeORM CLI DataSource)
- commit 58b2b09 — FOUND (feat(1.1-04): write baseline migration squashing full schema)
