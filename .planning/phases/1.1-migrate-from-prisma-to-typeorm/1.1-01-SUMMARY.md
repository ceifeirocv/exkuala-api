---
phase: 1.1
plan: 01
subsystem: entities
tags: [tdd, wave-0, typeorm, red-state]
dependency_graph:
  requires: []
  provides: [user.entity.spec.ts, event.entity.spec.ts]
  affects: [Plan 03 — entity implementation]
tech_stack:
  added: []
  patterns: [TDD Red-Green-Refactor, Wave 0 Nyquist compliance]
key_files:
  created:
    - src/users/user.entity.spec.ts
    - src/events/event.entity.spec.ts
  modified: []
decisions:
  - Spec stubs written before entities — tests drive Plan 03 implementation
  - RED state intentional: "Cannot find module" confirms no entity exists yet
metrics:
  duration: "~2 minutes"
  completed: 2026-04-19
  tasks_completed: 2
  files_created: 2
  files_modified: 0
---

# Phase 1.1 Plan 01: Wave 0 Entity Spec Stubs Summary

**One-liner:** TDD spec stubs for UserEntity CUID generation and EventEntity soft-delete behavior, in intentional RED state to drive Plan 03 entity implementation.

## What Was Built

Two failing test spec files were created as Wave 0 Nyquist compliance artifacts. They define the expected behavior of `UserEntity` and `EventEntity` **before** the entities exist, ensuring tests are never written after the fact.

### `src/users/user.entity.spec.ts` (MIGRATION-02)

Three test cases:
- `generateId` assigns a cuid-format string (20-30 chars) when `id` is unset
- `generateId` does NOT overwrite an existing `id` (guard check)
- Field shape: `id` and `auth0Id` properties are present on the entity

### `src/events/event.entity.spec.ts` (MIGRATION-03)

Four test cases:
- `generateId` assigns a cuid-format string (20-30 chars) when `id` is unset
- `generateId` does NOT overwrite an existing `id` (guard check)
- `EventStatus` enum has DRAFT, PUBLISHED, and CANCELLED values
- Field shape: `id`, `title`, `status`, `deletedAt`, and `organizerId` are present

## Wave 0 RED State Confirmed

```
FAIL src/users/user.entity.spec.ts
  Cannot find module './user.entity' from 'users/user.entity.spec.ts'

FAIL src/events/event.entity.spec.ts
  Cannot find module './event.entity' from 'events/event.entity.spec.ts'
```

This is the expected and correct outcome for Wave 0. Tests will turn GREEN after Plan 03 writes the entity implementations.

## Task Commits

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Create UserEntity spec stub (MIGRATION-02) | 8a0a20d | src/users/user.entity.spec.ts |
| 2 | Create EventEntity spec stub (MIGRATION-03) | a8c717e | src/events/event.entity.spec.ts |

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None — these files ARE the stubs (intentional Wave 0 artifacts). No production data paths involved.

## Threat Flags

None — test files have no production surface. T-1.1-W0-01 accepted per plan threat register.

## Self-Check: PASSED

- [x] `src/users/user.entity.spec.ts` exists
- [x] `src/events/event.entity.spec.ts` exists
- [x] Commit `8a0a20d` exists in git log
- [x] Commit `a8c717e` exists in git log
- [x] Both tests fail with "Cannot find module" (expected RED state)
