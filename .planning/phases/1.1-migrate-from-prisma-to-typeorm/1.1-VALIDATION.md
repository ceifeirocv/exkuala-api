---
phase: 1.1
slug: migrate-from-prisma-to-typeorm
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-19
---

# Phase 1.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Jest 30 + ts-jest |
| **Config file** | `jest` key in `package.json` |
| **Quick run command** | `npm test` |
| **Full suite command** | `npm run test:cov` |
| **Estimated runtime** | ~10 seconds |

---

## Sampling Rate

- **After every task commit:** Run `npm test`
- **After every plan wave:** Run `npm run test:cov`
- **Before `/gsd-verify-work`:** Full suite must be green + `npm run test:e2e` (AppModule boots cleanly)
- **Max feedback latency:** ~10 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 1.1-01-01 | 01 | 1 | MIGRATION-01 | T-synchronize | synchronize gated by NODE_ENV=development only | smoke (e2e) | `npm run test:e2e` | ✅ `test/app.e2e-spec.ts` | ⬜ pending |
| 1.1-01-02 | 01 | 1 | MIGRATION-02 | — | N/A | unit | `npm test -- --testPathPattern=user.entity` | ❌ W0 | ⬜ pending |
| 1.1-01-03 | 01 | 1 | MIGRATION-03 | — | softDelete excludes from find() automatically | unit | `npm test -- --testPathPattern=event.entity` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `src/users/user.entity.spec.ts` — MIGRATION-02: BeforeInsert assigns cuid-format id; `if (!this.id)` guard allows test-injected IDs
- [ ] `src/events/event.entity.spec.ts` — MIGRATION-03: `@DeleteDateColumn` sets deletedAt on softDelete; softDeleted rows excluded from standard find()

*Existing infrastructure (`test/app.e2e-spec.ts`) covers MIGRATION-01 — AppModule bootstrap.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `migration:run` applies baseline schema to a clean Postgres database | MIGRATION-01 | Requires live Postgres; not wirable in CI without a DB container | Run `npm run migration:run` against a local/dev DB; verify `users` and `events` tables created with all constraints |
| `npm install` succeeds without Prisma packages | Pitfall-5 | Requires full install cycle | Run `npm install` in a clean `node_modules`; confirm no "prisma: not found" errors in postinstall |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (`user.entity.spec.ts`, `event.entity.spec.ts`)
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
