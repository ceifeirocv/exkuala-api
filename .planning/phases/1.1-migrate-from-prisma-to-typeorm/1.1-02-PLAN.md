---
phase: 1.1
plan: 02
type: execute
wave: 1
depends_on: [1.1-01]
files_modified:
  - package.json
autonomous: true
requirements: [MIGRATION-01]

must_haves:
  truths:
    - "package.json has @nestjs/typeorm, typeorm, @paralleldrive/cuid2 in dependencies"
    - "package.json does NOT have @prisma/adapter-pg, @prisma/client, prisma in dependencies"
    - "postinstall script is removed from package.json"
    - "migration:generate, migration:run, migration:revert scripts are present"
    - "prisma:generate, prisma:migrate, prisma:studio scripts are removed"
  artifacts:
    - path: "package.json"
      provides: "Updated dependency manifest and migration scripts"
      contains: "@nestjs/typeorm"
  key_links:
    - from: "package.json scripts.migration:run"
      to: "dist/database/data-source.js"
      via: "npx typeorm migration:run -d"
      pattern: "dist/database/data-source\\.js"
---

<objective>
Install TypeORM packages, remove all Prisma packages, and update package.json scripts. This plan makes the dependency manifest accurate before any source files are modified.

Purpose: Package changes must happen first so subsequent plans can import from `typeorm` and `@nestjs/typeorm` without resolution errors.
Output: Updated package.json with correct dependencies and migration scripts. node_modules updated on disk.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/1.1-migrate-from-prisma-to-typeorm/1.1-RESEARCH.md
@.planning/phases/1.1-migrate-from-prisma-to-typeorm/1.1-CONTEXT.md
</context>

<interfaces>
<!-- Current package.json scripts and dependency sections — extracted for executor reference -->

Current scripts to REMOVE:
  "prisma:generate": "prisma generate"
  "prisma:migrate": "prisma migrate dev"
  "prisma:studio": "prisma studio"
  "postinstall": "prisma generate"

Current dependencies to REMOVE:
  "@prisma/adapter-pg": "^7.7.0"
  "@prisma/client": "^7.7.0"
  "prisma": "^7.7.0"

Scripts to ADD (exact strings from D-08 / RESEARCH.md Pattern 5):
  "migration:generate": "npm run build && npx typeorm migration:generate src/database/migrations/migration -d dist/database/data-source.js"
  "migration:run":      "npm run build && npx typeorm migration:run -d dist/database/data-source.js"
  "migration:revert":   "npm run build && npx typeorm migration:revert -d dist/database/data-source.js"

Packages to INSTALL:
  @nestjs/typeorm (11.0.1)
  typeorm (0.3.28)
  @paralleldrive/cuid2 (3.3.0)
  dotenv (check if already present as transitive dep; install explicitly if not)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install TypeORM packages and remove Prisma packages</name>
  <read_first>
    - package.json — current dependency state before any changes
  </read_first>
  <files>package.json</files>
  <action>
Run these commands in sequence from the project root (`/c/code/exkuala/exkuala-api`):

Step 1 — Install TypeORM stack (dotenv may already be a transitive dep, install explicitly to make it a direct dep):
```bash
npm install @nestjs/typeorm typeorm @paralleldrive/cuid2 dotenv
```

Step 2 — Remove Prisma packages. NOTE: `postinstall: "prisma generate"` still exists, so npm install will try to run it. The uninstall must happen while Prisma binaries are still present OR the postinstall must be removed from package.json BEFORE running npm uninstall. Remove postinstall from package.json first by editing the file directly, THEN run the uninstall.

Edit package.json: delete the `"postinstall": "prisma generate"` line from the `"scripts"` section.

Then run:
```bash
npm uninstall @prisma/adapter-pg @prisma/client prisma
```

Verify step 2 succeeds without postinstall hook errors.
  </action>
  <verify>
    <automated>cd /c/code/exkuala/exkuala-api && node -e "const p=require('./package.json'); const deps=Object.keys(p.dependencies||{}); console.log('HAS_TYPEORM='+deps.includes('@nestjs/typeorm')); console.log('HAS_PRISMA='+deps.includes('prisma'));"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "require('@nestjs/typeorm')"` exits 0 (package loadable from node_modules)
    - `node -e "require('typeorm')"` exits 0
    - `node -e "require('@paralleldrive/cuid2')"` exits 0
    - `node -e "require('dotenv')"` exits 0
    - grep -q '"@nestjs/typeorm"' package.json
    - grep -q '"typeorm"' package.json
    - grep -q '"@paralleldrive/cuid2"' package.json
    - grep -q '"dotenv"' package.json
    - grep -vq '"@prisma/adapter-pg"' package.json
    - grep -vq '"@prisma/client"' package.json
    - grep -vq '"prisma"' package.json (as a dependency key)
    - grep -vq '"postinstall"' package.json
  </acceptance_criteria>
  <done>TypeORM packages installed in node_modules and reflected in package.json. Prisma packages removed. postinstall hook deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Update package.json scripts (add migration scripts, remove Prisma scripts)</name>
  <read_first>
    - package.json — current scripts section after Task 1 (postinstall already removed)
  </read_first>
  <files>package.json</files>
  <action>
Edit `package.json` scripts section to make the following changes:

REMOVE these scripts (if any remain after Task 1):
- `"prisma:generate": "prisma generate"`
- `"prisma:migrate": "prisma migrate dev"`
- `"prisma:studio": "prisma studio"`

ADD these scripts (exact strings — do not paraphrase):
```json
"migration:generate": "npm run build && npx typeorm migration:generate src/database/migrations/migration -d dist/database/data-source.js",
"migration:run":      "npm run build && npx typeorm migration:run -d dist/database/data-source.js",
"migration:revert":   "npm run build && npx typeorm migration:revert -d dist/database/data-source.js"
```

After editing, verify package.json is valid JSON by running:
```bash
node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))" && echo "VALID JSON"
```

The final scripts section should contain: build, format, start, start:dev, start:debug, start:prod, lint, test, test:watch, test:cov, test:debug, test:e2e, migration:generate, migration:run, migration:revert.
  </action>
  <verify>
    <automated>cd /c/code/exkuala/exkuala-api && node -e "const s=require('./package.json').scripts; console.log('MIGRATION_RUN='+!!s['migration:run']); console.log('NO_PRISMA_STUDIO='+!s['prisma:studio']); console.log('NO_POSTINSTALL='+!s['postinstall']);"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "migration:generate" package.json
    - grep -q "migration:run" package.json
    - grep -q "migration:revert" package.json
    - grep -q "dist/database/data-source.js" package.json
    - grep -vq "prisma:generate" package.json
    - grep -vq "prisma:migrate" package.json
    - grep -vq "prisma:studio" package.json
    - `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` exits 0 (valid JSON)
    - `npm run migration:run --dry-run 2>&1 | grep -q "migration:run"` — script is registered (will fail to connect to DB, but that's expected here)
  </acceptance_criteria>
  <done>package.json scripts section has migration:generate, migration:run, migration:revert pointing at dist/database/data-source.js. All Prisma scripts removed. JSON is valid.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → node_modules | Package download from public registry |
| package.json postinstall hook | Was running prisma generate automatically on every npm install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1.1-02-01 | Tampering | package.json postinstall | mitigate | Remove "postinstall": "prisma generate" before uninstalling Prisma — prevents npm install failures in CI/CD |
| T-1.1-02-02 | Denial of Service | npm uninstall | accept | Uninstall while Prisma binaries present is safe; race condition not possible in single-threaded npm |
</threat_model>

<verification>
After both tasks:
- `node -e "require('@nestjs/typeorm')"` exits 0
- `node -e "require('@paralleldrive/cuid2')"` exits 0
- `node -e "require('dotenv')"` exits 0
- `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` exits 0
- `grep "migration:run" package.json` outputs the build-first command
- `grep "prisma" package.json` — only matches comments, not dependency keys or script values (Prisma fully removed)
- `npm test` — existing passing tests remain green (app.controller.spec.ts)
</verification>

<success_criteria>
TypeORM installed, Prisma removed, migration scripts wired. `npm test` passes (app.controller.spec.ts green). No postinstall hook. package.json is valid JSON.
</success_criteria>

<output>
After completion, create `.planning/phases/1.1-migrate-from-prisma-to-typeorm/1.1-02-SUMMARY.md`
</output>
